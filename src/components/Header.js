import React from 'react';

const styles = {
  backgroundColor: '#222',
  height: '75px',
  padding: '20px',
  color: '#fff',
}

const Header = () => (
  <div style={styles}>
    <h2>ACLU Donation Multiplier</h2>
  </div>
);

export default Header;